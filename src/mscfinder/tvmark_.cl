sleep (1)
if (access (coords) == YES)
    tv.tvmark (frame=frame, coords=coords, mark=mark, radii=radii,
	lengths=lengths, color=color, label=label, number=number,
	pointsize=pointsize, txsize=txsize, nxoffset=0, nyoffset=0,
	logfile="", autolog-, outimage="", deletions="", commands="",
	font="raster", tolerance=1.5, interactive-)
;
